# Neon Postgres — same database the backend/API uses (pooled connection string).
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Secret for the admin panel's own session cookie. Generate with: openssl rand -base64 48
ADMIN_JWT_SECRET=""
