# Base URL of the NxtGenProperties Hono API (shared with the Expo app & admin panel).
# Local dev default is http://localhost:4000. For a deployed backend, set the public URL.
PUBLIC_API_URL="http://localhost:4000"
